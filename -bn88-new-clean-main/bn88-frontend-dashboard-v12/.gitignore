# --- Node/General ---
node_modules/
dist/
build/
coverage/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store

# --- IDE/Editor (ถ้าทีมไม่ต้องแชร์) ---
.vscode/
.idea/

# --- Prisma / DB ---
prisma/*.db
prisma/*.db-journal
!prisma/migrations/**   # <- อนุญาตไฟล์ migrations

# --- Env secrets ---
.env
.env.local
.env.*.local
!.env.example           # <- อนุญาตไฟล์ตัวอย่าง

# --- Frontend-build (ถ้ามี) ---
*.tsbuildinfo
