# Local Development Ports

- Dashboard: `http://localhost:5555` (`http://127.0.0.1:5555`)
- Backend API: `http://localhost:3000` (`http://127.0.0.1:3000`)

## CORS (Backend)

Set `ALLOWED_ORIGINS` (comma-separated) to include at least:

- `http://localhost:5555`
- `http://127.0.0.1:5555`
